{
  "private": true,
  "workspaces": [
    "react-menu-hook",
    "website"
  ],
  "scripts": {
    "test": "cd react-menu-hook && yarn test",
    "type-check": "cd react-menu-hook && yarn type-check && cd ../website && yarn type-check"
  }
}
